import { StyleSheet } from 'react-native';

export const GlobalStyles = (theme: any, fontFamily: any, fontSize: any) => StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "white",
  },
});
